package controller.rule

import model.impl.PlayerNameEnum.PlayerNameEnum
import util.position.Position

object Postcondition {

  def isATileNoTrapped(playerName: PlayerNameEnum, posFrom: Position): Boolean = {
    false
  }
}
